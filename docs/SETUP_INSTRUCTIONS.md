# 🚀 Инструкция по запуску FileStorage Bot

## 📋 Предварительные требования

1. **Python 3.8+** (рекомендуется 3.11+)
2. **Telegram Bot Token** (получить у @BotFather)

## 🔧 Настройка

### 1. Получение токена бота

1. Откройте Telegram
2. Найдите @BotFather
3. Отправьте команду `/newbot`
4. Следуйте инструкциям для создания бота
5. Скопируйте полученный токен

### 2. Настройка конфигурации

Создайте файл `.env` в корневой папке проекта:

```bash
# Telegram Bot Token
BOT_TOKEN=ваш_токен_бота_здесь

# Максимальный размер файла в байтах (50MB по умолчанию)
MAX_FILE_SIZE=52428800

# Уровень логирования (INFO, DEBUG, WARNING, ERROR)
LOG_LEVEL=INFO
```

### 3. Установка зависимостей

```bash
# Активируйте виртуальное окружение (если используется)
source .venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

## 🚀 Запуск бота

### Способ 1: Прямой запуск
```bash
python3 main.py
```

### Способ 2: Через модуль
```bash
python3 -m src.bot.main
```

## 📱 Использование бота

### Основные команды:
- `/start` - Главное меню
- `/upload` - Загрузить файл
- `/files` - Показать ваши файлы
- `/search` - Поиск файлов
- `/delete` - Удаление файлов
- `/export` - Экспорт файлов
- `/stats` - Статистика
- `/help` - Помощь

### Функции:
- 📁 **Загрузка файлов** - отправьте любой файл боту
- 📋 **Категории** - файлы автоматически сортируются по типам
- 🔍 **Поиск** - найдите файлы по названию или тегам
- 🔗 **Поделиться** - создайте ссылку для скачивания файла
- 📊 **Статистика** - просмотр статистики использования
- 📤 **Экспорт** - скачайте список файлов в CSV
- 🗑️ **Удаление** - удалите ненужные файлы

## 🔧 Конфигурация

### Переменные окружения:

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `BOT_TOKEN` | Токен Telegram бота | Обязательно |
| `MAX_FILE_SIZE` | Максимальный размер файла (байты) | 52428800 (50MB) |
| `LOG_LEVEL` | Уровень логирования | INFO |

## 📊 Логи

Логи сохраняются в папке `logs/`:
- `bot.log` - основные логи бота
- `database.log` - логи базы данных

## 🛠️ Устранение неполадок

### Ошибка "Token is invalid!"
- Проверьте правильность токена в файле `.env`
- Убедитесь, что токен скопирован полностью

### Ошибка "Module not found"
- Установите зависимости: `pip install -r requirements.txt`
- Активируйте виртуальное окружение

### Файлы не загружаются
- Проверьте размер файла (максимум 50MB по умолчанию)
- Убедитесь, что у бота есть права на получение файлов

## 📈 Мониторинг

Бот автоматически создает:
- Логи в папке `logs/`
- Базу данных SQLite
- Статистику использования

## 🔄 Обновления

Для обновления бота:
1. Остановите бота (Ctrl+C)
2. Обновите код
3. Установите новые зависимости: `pip install -r requirements.txt`
4. Запустите бота заново

---
*Бот готов к работе! 🎉* 