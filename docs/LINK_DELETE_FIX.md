# Исправление проблемы с удалением ссылок

## Проблема

При попытке удалить ссылку бот показывал сообщение "Файл не найден" вместо правильной обработки удаления ссылки.

## Причина

Обработчик `callback_delete_file` перехватывал все callback'и, начинающиеся с "delete_", включая "delete_link_". Это приводило к тому, что при нажатии кнопки удаления ссылки система пыталась обработать `link_1` как ID файла, что вызывало ошибку:

```
invalid literal for int() with base 10: 'link_1'
```

## Решение

### Изменения в `src/handlers/handlers.py`

#### 1. Исправление обработчика удаления файлов

**Было:**
```python
@router.callback_query(F.data.startswith("delete_"))
async def callback_delete_file(callback: CallbackQuery):
```

**Стало:**
```python
@router.callback_query(F.data.startswith("delete_") & ~F.data.startswith("delete_link_"))
async def callback_delete_file(callback: CallbackQuery):
```

#### 2. Исправление обработчика подтверждения удаления файлов

**Было:**
```python
@router.callback_query(F.data.startswith("confirm_delete_"))
async def callback_confirm_delete(callback: CallbackQuery):
```

**Стало:**
```python
@router.callback_query(F.data.startswith("confirm_delete_") & ~F.data.startswith("confirm_delete_link_"))
async def callback_confirm_delete(callback: CallbackQuery):
```

### Объяснение

Добавлены условия исключения для правильного разделения обработчиков:

1. **Удаление файлов**: `~F.data.startswith("delete_link_")` - исключает callback'и удаления ссылок
2. **Подтверждение удаления файлов**: `~F.data.startswith("confirm_delete_link_")` - исключает callback'и подтверждения удаления ссылок

Теперь:
- `delete_123` → обрабатывается как удаление файла
- `delete_link_456` → обрабатывается как удаление ссылки
- `confirm_delete_123` → обрабатывается как подтверждение удаления файла
- `confirm_delete_link_456` → обрабатывается как подтверждение удаления ссылки

## Результат

Теперь удаление ссылок работает корректно:

1. **Пользователь нажимает** кнопку "🗑️ Удалить" рядом со ссылкой
2. **Бот показывает** подтверждение удаления
3. **При подтверждении** ссылка удаляется из базы данных
4. **Показывается** сообщение об успешном удалении

### Пример работы

```
🗑️ Вы уверены, что хотите удалить ссылку **Google**?
[✅ Да, удалить] [❌ Отменить]

✅ Ссылка успешно удалена!
[🔗 Мои ссылки] [🏠 Главное меню]
```

## Технические детали

### Обработчики удаления

1. **`callback_delete_file`** - удаление файлов (callback'и `delete_*`)
2. **`callback_confirm_delete`** - подтверждение удаления файлов (callback'и `confirm_delete_*`)
3. **`callback_delete_link`** - удаление ссылок (callback'и `delete_link_*`)
4. **`callback_confirm_delete_link`** - подтверждение удаления ссылок (callback'и `confirm_delete_link_*`)

### Логика работы

```python
# Для файлов
@router.callback_query(F.data.startswith("delete_") & ~F.data.startswith("delete_link_"))
async def callback_delete_file(callback: CallbackQuery):
    record_id = callback.data.replace("delete_", "")
    # Обработка удаления файла

@router.callback_query(F.data.startswith("confirm_delete_") & ~F.data.startswith("confirm_delete_link_"))
async def callback_confirm_delete(callback: CallbackQuery):
    record_id = callback.data.replace("confirm_delete_", "")
    # Обработка подтверждения удаления файла

# Для ссылок  
@router.callback_query(F.data.startswith("delete_link_"))
async def callback_delete_link(callback: CallbackQuery):
    link_id = int(callback.data.replace("delete_link_", ""))
    # Обработка удаления ссылки

@router.callback_query(F.data.startswith("confirm_delete_link_"))
async def callback_confirm_delete_link(callback: CallbackQuery):
    link_id = int(callback.data.replace("confirm_delete_link_", ""))
    # Обработка подтверждения удаления ссылки
```

## Тестирование

Исправление протестировано:
- ✅ Удаление файлов работает корректно
- ✅ Удаление ссылок работает корректно
- ✅ Нет конфликтов между обработчиками
- ✅ Правильные сообщения об ошибках

## Преимущества

1. **Разделение логики** - файлы и ссылки обрабатываются отдельно
2. **Предотвращение ошибок** - нет путаницы между типами объектов
3. **Четкая структура** - каждый обработчик отвечает за свой тип данных
4. **Удобство отладки** - легче найти и исправить проблемы 