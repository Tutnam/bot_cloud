# 🔧 Утилиты управления ботом

## Обзор

В проекте есть утилиты для управления и отладки бота без необходимости запуска основного приложения.

## 📋 Доступные утилиты

### reset_bot.py

Утилита для управления ботом через Telegram Bot API.

#### Использование:

```bash
# Показать справку
python reset_bot.py

# Получить информацию о боте
python reset_bot.py info

# Сбросить webhook
python reset_bot.py reset
```

#### Команды:

| Команда | Описание |
|---------|----------|
| `info` | Получить информацию о боте |
| `reset` | Сбросить webhook и очистить обновления |

#### Примеры вывода:

**Информация о боте:**
```
📊 Получение информации о боте...
🤖 Информация о боте:
   Имя: Pars_school_bot
   Username: @pars_school_bot
   ID: 8359217636
   Поддерживает inline: False
```

**Сброс webhook:**
```
🔄 Сброс webhook...
✅ Webhook сброшен успешно
🤖 Информация о боте:
   Имя: Pars_school_bot
   Username: @pars_school_bot
   ID: 8359217636
🌐 Webhook не установлен
```

## 🔧 Технические детали

### Особенности реализации:

- **Прямые API вызовы** - не использует aiogram для избежания проблем с зависимостями
- **Проверка .env** - автоматическая проверка наличия токена
- **Обработка ошибок** - подробные сообщения об ошибках
- **Безопасность** - не сохраняет токен в логах

### Используемые API методы:

- `getMe` - получение информации о боте
- `deleteWebhook` - сброс webhook
- `getWebhookInfo` - получение информации о webhook

### Зависимости:

- `requests` - HTTP запросы к API
- `python-dotenv` - загрузка переменных окружения
- `json` - обработка JSON ответов

## 🚀 Использование в разработке

### Отладка проблем:

```bash
# Проверить статус бота
python reset_bot.py info

# Очистить webhook при проблемах
python reset_bot.py reset

# Перезапустить бота после сброса
python main.py
```

### Типичные сценарии:

1. **Бот не отвечает** - сбросить webhook
2. **Проверить токен** - получить информацию о боте
3. **Отладка webhook** - проверить статус webhook
4. **Очистка обновлений** - удалить накопившиеся обновления

## ⚠️ Важные замечания

### Безопасность:
- Токен бота загружается из .env файла
- Не передается в аргументах командной строки
- Не сохраняется в логах

### Ограничения:
- Требует валидный токен бота
- Работает только с polling режимом
- Не поддерживает webhook режим

### Требования:
- Файл .env с BOT_TOKEN
- Установленный python-dotenv
- Доступ к интернету для API запросов

## 🔄 Интеграция с основным проектом

### Автоматизация:

Можно добавить в CI/CD:
```yaml
- name: Reset bot webhook
  run: python reset_bot.py reset
```

### Скрипты развертывания:

```bash
#!/bin/bash
# deploy.sh
echo "🔄 Сброс webhook..."
python reset_bot.py reset

echo "🚀 Запуск бота..."
python main.py
```

---

**Статус:** ✅ Готово к использованию  
**Версия:** 1.2.1  
**Дата:** 03.08.2025 