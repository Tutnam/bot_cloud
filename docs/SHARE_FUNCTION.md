# Функция "Поделиться файлом" - Генерация ссылки для скачивания

## Описание

Функция "Поделиться файлом" позволяет пользователям создавать безопасные временные ссылки для скачивания своих файлов. Бот предоставляет **безопасную ссылку через бота**, которая:

- Показывает информацию о файле
- Автоматически отправляет файл при переходе
- Обеспечивает безопасность и контроль доступа

## Основные возможности

### 🔗 Создание ссылок
- Генерация уникальных безопасных ссылок для каждого файла
- **Ссылки через бота** с информацией о файле и автоматической отправкой
- Срок действия ссылки: 24 часа
- Безопасность: только владелец файла может создать ссылку
- Защита токена бота от раскрытия

### 📥 Скачивание по ссылке
- **Ссылка через бота**: показывает информацию и сразу отправляет файл
- Поддержка всех типов файлов
- Безопасный доступ через Telegram Bot API

### 🛡️ Безопасность
- Проверка срока действия ссылок
- Автоматическая деактивация истекших ссылок
- Доступ только к активным ссылкам
- Защита токена бота от раскрытия
- Контроль доступа через Telegram Bot API

## Техническая реализация

### База данных

#### Таблица `share_links`
```sql
CREATE TABLE share_links (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    share_id TEXT UNIQUE NOT NULL,
    file_id TEXT NOT NULL,
    user_id INTEGER NOT NULL,
    record_id INTEGER NOT NULL,
    created_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    expires_date TIMESTAMP,
    is_active BOOLEAN DEFAULT 1,
    FOREIGN KEY (record_id) REFERENCES files (id) ON DELETE CASCADE
);
```

### Основные методы

#### `add_share_link(share_id, file_id, user_id, record_id)`
Создает новую ссылку на файл.

**Параметры:**
- `share_id` (str): Уникальный идентификатор ссылки
- `file_id` (str): Telegram file_id
- `user_id` (int): ID пользователя-владельца
- `record_id` (int): ID записи файла в базе

**Возвращает:** `bool` - успешность операции

#### `get_share_link(share_id)`
Получает информацию о ссылке с проверкой срока действия.

**Параметры:**
- `share_id` (str): Идентификатор ссылки

**Возвращает:** `tuple` или `None` - данные ссылки или None если не найдена/истекла

#### `deactivate_share_link(share_id)`
Деактивирует ссылку.

**Параметры:**
- `share_id` (str): Идентификатор ссылки

**Возвращает:** `bool` - успешность операции

### Обработчики

#### `callback_share_file()`
Обработчик кнопки "Поделиться" для создания ссылки.

**Функции:**
- Генерация уникального share_id
- Создание ссылки в базе данных
- Формирование URL для скачивания с реальным именем бота
- Отображение информации о ссылке

#### `get_bot_share_url(share_id)`
Вспомогательная функция для генерации ссылки через бота.

**Параметры:**
- `share_id` (str): Идентификатор ссылки

**Возвращает:** `str` - ссылка через бота с реальным именем бота



#### `handle_shared_file_download()`
Обработчик перехода по ссылке на файл.

**Функции:**
- Проверка валидности ссылки
- Отображение информации о файле
- Предоставление кнопки для скачивания

#### `callback_download_shared_file()`
Обработчик скачивания файла по ссылке.

**Функции:**
- Проверка активности ссылки
- Отправка файла пользователю
- Логирование скачивания

## Пользовательский интерфейс

### Создание ссылки
1. Пользователь нажимает кнопку "🔗 Поделиться" рядом с файлом
2. Бот генерирует уникальную ссылку и получает прямую ссылку на файл
3. Отображается информация о ссылках:
   ```
   🔗 Ссылка для скачивания файла
   
   📄 Файл: document.pdf
   📏 Размер: 2.5 MB
   📅 Загружен: 15.12.2023 14:30
   
   🔗 Ссылка для скачивания:
   https://t.me/pars_school_bot?start=file_abc123def456
   
   💡 Как использовать:
   1. Скопируйте ссылку и отправьте другу
   2. Или просто перешлите это сообщение другу
   3. При переходе по ссылке файл будет отправлен автоматически
   
   ⚠️ Внимание: Ссылка действительна 24 часа
   ```

### Скачивание по ссылке
1. Пользователь переходит по ссылке
2. Отображается информация о файле:
   ```
   🔗 Файл по ссылке
   
   📄 Название: document.pdf
   📏 Размер: 2.5 MB
   📁 Тип: pdf
   📅 Загружен: 15.12.2023 14:30
   ⏰ Действует до: 16.12.2023 14:30
   
   📝 Описание: Важный документ
   🏷️ Теги: документ, работа
   ```
3. Кнопка "📥 Скачать файл" для получения файла

## Безопасность

### Ограничения доступа
- Только владелец файла может создать ссылку
- Ссылки автоматически истекают через 24 часа
- Неактивные ссылки не работают

### Валидация
- Проверка существования файла
- Проверка срока действия ссылки
- Проверка активности ссылки

### Логирование
- Создание ссылок
- Переходы по ссылкам
- Скачивания файлов
- Ошибки и исключения

## Интеграция с существующим кодом

### Обновления в handlers.py
- Добавлена кнопка "🔗 Поделиться" в список файлов
- Обновлен обработчик команды `/start` для обработки параметров
- Добавлены новые callback обработчики
- Добавлена функция `get_bot_share_url()` для генерации ссылок с реальным именем бота

### Обновления в database.py
- Создана таблица `share_links`
- Добавлены методы для работы со ссылками
- Реализована автоматическая очистка истекших ссылок

### Обновления в config.py
- Добавлено поле `BOT_USERNAME` для хранения имени бота
- Добавлен метод `get_bot_info()` для получения информации о боте при запуске

### Обновления в bot/main.py
- Добавлено получение информации о боте при запуске
- Установка `BOT_USERNAME` в конфигурации

### Обновления в help и main menu
- Добавлена информация о функции шаринга
- Обновлены описания команд

## Тестирование

### Тесты базы данных
- `test_add_share_link()` - тест создания ссылки
- `test_get_share_link()` - тест получения ссылки
- `test_deactivate_share_link()` - тест деактивации ссылки
- `test_get_bot_share_url()` - тест генерации ссылки с именем бота

### Сценарии использования
1. Создание ссылки на файл
2. Переход по ссылке
3. Скачивание файла
4. Проверка истечения срока действия
5. Деактивация ссылки

## Будущие улучшения

### Возможные расширения
- Настраиваемый срок действия ссылок
- Ограничение количества скачиваний
- Пароли для ссылок
- Статистика использования ссылок
- Массовое создание ссылок

### Оптимизации
- Кэширование активных ссылок
- Периодическая очистка истекших ссылок
- Улучшенная валидация ссылок 