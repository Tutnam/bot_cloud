# 🤖 FileStorage Bot - Telegram файлохранилище

Telegram бот для хранения и управления файлами прямо в Telegram. Файлы сохраняются в Telegram и доступны для скачивания через удобный интерфейс бота.

## ✨ Возможности

- 📤 **Загрузка файлов** - Поддержка документов, фото, видео, аудио
- 📁 **Управление файлами** - Просмотр, поиск, скачивание
- 🏷️ **Организация** - Добавление описаний и тегов к файлам
- 🔍 **Поиск** - Поиск по названию и описанию файлов
- 📊 **Статистика** - Информация о количестве и размере файлов
- 💾 **Безопасность** - Проверка размера файлов
- 🎨 **Удобный интерфейс** - Инлайн-клавиатуры и эмодзи
- 📥 **Скачивание** - Быстрое скачивание файлов через кнопки

## 🚀 Установка и запуск

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd bot_cloud
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Создание бота в Telegram
1. Найдите @BotFather в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен

### 4. Настройка конфигурации
Создайте файл `.env` в корне проекта:
```env
BOT_TOKEN=ваш_токен_бота_здесь
MAX_FILE_SIZE=52428800
LOG_LEVEL=INFO
```

### 5. Запуск бота
```bash
python main.py
```

## 📁 Структура проекта

```
bot_cloud/
├── main.py              # Главный файл бота
├── config.py            # Конфигурация
├── database.py          # Работа с базой данных
├── handlers.py          # Обработчики команд
├── utils.py             # Утилиты
├── requirements.txt     # Зависимости
├── README.md           # Документация
├── .env                # Переменные окружения (создать)
├── files.db            # База данных SQLite (создается автоматически)
└── bot.log             # Лог файл (создается автоматически)
```

## 🎯 Команды бота

### Основные команды
- `/start` - Запуск бота и главное меню
- `/help` - Справка по использованию
- `/files` - Показать ваши файлы
- `/stats` - Статистика использования
- `/search <запрос>` - Поиск файлов

### Загрузка файлов
Просто отправьте файл в чат с ботом, и он автоматически сохранит его в Telegram.

### Скачивание файлов
Используйте кнопки "📥 Скачать" в списке файлов для быстрого скачивания.

## ⚙️ Конфигурация

### Переменные окружения (.env)

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `BOT_TOKEN` | Токен Telegram бота | - |
| `MAX_FILE_SIZE` | Максимальный размер файла (байты) | `52428800` (50MB) |
| `LOG_LEVEL` | Уровень логирования | `INFO` |

## 🗄️ База данных

Бот использует SQLite для хранения информации о файлах. Файлы физически хранятся в Telegram, а в базе данных сохраняется только метаинформация. Таблица `files` содержит:

- `id` - Уникальный идентификатор
- `file_id` - Telegram file_id для доступа к файлу
- `file_name` - Название файла
- `file_size` - Размер файла
- `file_type` - Тип файла
- `user_id` - ID пользователя
- `upload_date` - Дата загрузки
- `description` - Описание файла
- `tags` - Теги файла
- `message_id` - ID сообщения с файлом
- `chat_id` - ID чата

## 🔧 Разработка

### Добавление новых типов файлов
1. Добавьте расширение в `ALLOWED_EXTENSIONS` в `.env`
2. Добавьте MIME-тип в `mime_to_ext` в `utils.py`
3. Добавьте иконку в `get_file_type_icon` в `utils.py`

### Добавление новых команд
1. Создайте обработчик в `handlers.py`
2. Зарегистрируйте его в роутере
3. Добавьте описание в `/help` команду

## 🛡️ Безопасность

- Проверка типов файлов
- Ограничение размера файлов
- Валидация имен файлов
- Изоляция файлов пользователей
- Логирование всех операций

## 📝 Логирование

Бот ведет подробные логи в файле `bot.log`:
- Информация о загрузке файлов
- Ошибки и исключения
- Статистика использования
- Действия пользователей

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте Pull Request

## 📄 Лицензия

MIT License

## 🆘 Поддержка

Если у вас возникли проблемы:
1. Проверьте логи в `bot.log`
2. Убедитесь, что токен бота правильный
3. Проверьте настройки в `.env`
4. Создайте Issue в репозитории

---

**Автор:** [Ваше имя]  
**Версия:** 1.0.0  
**Дата:** 2024 